@model CH.Spartan.Nodes.Dto.CreateNodeOutput
@section scripts
{
    @Scripts.Render("~/scripts/plugins/edit")
    <script type="text/javascript">
        var from = $("#form");
        (function ($) {
            if (!$) {
                return;
            }
            from.validate({
                submitHandler: function (form) {
                    var data =
                    {
                        node: abp.utils.getFormData(form)
                    };
                    abp.ajax({
                        url: "/SystemManage/CreateNode",
                        data: JSON.stringify(data)
                    }).done(function (result) {
                        abp.message.success("@L("添加成功")");
                        abp.dialog.close();
                    });
                }
            });
        })(jQuery);

        function submitForm() {
            from.submit();
        }
    </script>
}

<div class="ibox float-e-margins animated lightSpeedIn">
    <form class="form-horizontal" id="form">
        <div class="ibox-content">
            <div class="row">
                <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
                    <div class="form-group">
                        <label class="col-sm-4 control-label">@L("节点名字"):</label>
                        <div class="col-sm-8 ">
                            <input class="form-control " type="text" id="Name" name="Name" required value="@Model.Node.Name" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-4 control-label">@L("历史数据表"):</label>
                        <div class="col-sm-8">
                            <input class="form-control " type="text" id="HistoryTableName" required name="HistoryTableName" value="@Model.Node.HistoryTableName" />
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </form>
</div>

